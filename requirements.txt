aiohttp
ntplib
fastapi
uvicorn[standard]
argon2-cffi
aiosqlite
cryptography
# NameMC drop-window autofill is optional and home-machine only (Cloudflare
# blocks datacenter IPs). For it: pip install -r requirements-namemc.txt
